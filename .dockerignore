# Applies to builds whose context is the repo root (client-mqtt/, client-long-poll/,
# client-ws/, client-sse/ — see docker-compose.yml). service/, login-consent/, and
# client-admin/ each build with their own directory as context and have their own
# .dockerignore instead.
client-mqtt/config.js
client-long-poll/config.js
client-ws/config.js
client-sse/config.js
.git/
