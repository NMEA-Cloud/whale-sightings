# Used when running the service outside Docker (e.g. via uvicorn --reload)
VALKEY_HOST=localhost
VALKEY_PORT=6379
MQTT_HOST=localhost
MQTT_PORT=1883
CORS_ORIGINS=http://localhost:8080,http://localhost:8081
