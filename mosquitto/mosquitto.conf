# Local dev/demo broker: anonymous access, no TLS/auth (matches the project's local-dev
# posture — see certs/ for the one place auth/TLS *does* matter, the HTTP API).
listener 1883
protocol mqtt
allow_anonymous true

listener 9001
protocol websockets
allow_anonymous true

persistence false
log_dest stdout
